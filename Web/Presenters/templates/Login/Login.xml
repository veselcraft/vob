{extends "../@layout.xml"}

{block title}Login{/block}

{block content}
<div class="hero py-2">
    <div class="container">
        <h1>{_sign_in}</h1>
        <form method="post">
            <div class="form-group">
                <label for="email">{_email}</label>
                <input type="text" class="form-control" name="email" aria-describedby="emailHelp" required>
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">{_password}</label>
                <input type="password" class="form-control" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary">{_sign_in}</button>
        </form>
    </div>
</div>
{/block}
